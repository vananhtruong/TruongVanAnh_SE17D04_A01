@model BusinessObjects.Models.SystemAccount

@{
    ViewData["Title"] = "Create New Account";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container my-4">
    <div class="card shadow-sm">
        <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
            <h5 class="mb-0">Create New Account</h5>
            <a asp-action="Index" class="btn btn-sm btn-outline-light">
                <i class="bi bi-arrow-left me-1"></i> Back to List
            </a>
        </div>
        <div class="card-body p-4">
            <form asp-action="Create" method="post">
                <!-- Validation Summary -->
                <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-4" role="alert"></div>

                <!-- Account ID -->
                <div class="row mb-3">
                    <label asp-for="AccountId" class="col-md-3 col-form-label fw-bold"></label>
                    <div class="col-md-9">
                        <input asp-for="AccountId" class="form-control" placeholder="Enter account ID" />
                        <span asp-validation-for="AccountId" class="text-danger"></span>
                    </div>
                </div>

                <!-- Account Name -->
                <div class="row mb-3">
                    <label asp-for="AccountName" class="col-md-3 col-form-label fw-bold"></label>
                    <div class="col-md-9">
                        <input asp-for="AccountName" class="form-control" placeholder="Enter account name" />
                        <span asp-validation-for="AccountName" class="text-danger"></span>
                    </div>
                </div>

                <!-- Account Email -->
                <div class="row mb-3">
                    <label asp-for="AccountEmail" class="col-md-3 col-form-label fw-bold"></label>
                    <div class="col-md-9">
                        <input asp-for="AccountEmail" class="form-control" type="email" placeholder="Enter email address" />
                        <span asp-validation-for="AccountEmail" class="text-danger"></span>
                    </div>
                </div>

                <!-- Account Role (Dropdown) -->
                <div class="row mb-3">
                    <label asp-for="AccountRole" class="col-md-3 col-form-label fw-bold"></label>
                    <div class="col-md-9">
                        <select asp-for="AccountRole" class="form-select" asp-items="@((IEnumerable<SelectListItem>)ViewBag.RoleList)">
                            <option value="">-- Select Role --</option>
                        </select>
                        <span asp-validation-for="AccountRole" class="text-danger"></span>
                    </div>
                </div>

                <!-- Account Password -->
                <div class="row mb-3">
                    <label asp-for="AccountPassword" class="col-md-3 col-form-label fw-bold"></label>
                    <div class="col-md-9">
                        <input asp-for="AccountPassword" class="form-control" type="password" placeholder="Enter password" />
                        <span asp-validation-for="AccountPassword" class="text-danger"></span>
                    </div>
                </div>

                <!-- Confirm Password -->
                <div class="row mb-4">
                    <label for="confirmPassword" class="col-md-3 col-form-label fw-bold">Confirm Password</label>
                    <div class="col-md-9">
                        <input id="confirmPassword" name="confirmPassword" class="form-control" type="password" placeholder="Confirm password" />
                        <span class="text-danger" id="confirm-password-error"></span>
                    </div>
                </div>

                <!-- Buttons -->
                <div class="d-flex justify-content-end gap-2">
                    <button type="submit" class="btn btn-success">
                        <i class="bi bi-check-circle me-1"></i> Create
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="bi bi-x-circle me-1"></i> Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        $(document).ready(function () {
            $('form').on('submit', function (e) {
                var password = $('#AccountPassword').val();
                var confirmPassword = $('#confirmPassword').val();

                if (password !== confirmPassword) {
                    e.preventDefault();
                    $('#confirm-password-error').text('Passwords do not match.');
                } else {
                    $('#confirm-password-error').text('');
                }
            });
        });
    </script>
}